import React from 'react';
/*
const List = (items)=>{
   
    return(
        <div className="list">
            {
                //items.item.map(({value})=><p>{value}</p>)
                items.item.forEach(([type, link]) =><a href={link}>{type}</a>)
            }
        </div>
    );
};
const TextWithDropdown = (props) => {
    return (
        <div>
            <p className="type">{props.title}</p>
            <List className="input" item={props.items}></List>
        </div>
    );
};

export default TextWithDropdown;*/

//
//I will use this component
//https://www.npmjs.com/package/react-dropdown-input